# Data model change log

Source of truth: `src/db/schema.sql` (P1 owns changes). Log one line per
column/table added so nobody silently diverges (README section 8).

- (none yet — initial schema matches README section 8 verbatim)
