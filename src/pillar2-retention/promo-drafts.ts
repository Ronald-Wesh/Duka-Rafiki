// P2 owns this. Drafts a promo message via askClaude("draft-promo", ...)
// for the weekly "your regulars" summary.
export async function draftPromo(_customerIds: number[]): Promise<string> {
  throw new Error("not implemented — P2");
}
